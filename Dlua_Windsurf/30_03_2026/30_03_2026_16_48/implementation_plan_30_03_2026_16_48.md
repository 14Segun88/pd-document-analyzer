# План реализации: Улучшение извлечения метаданных

Цель — устранить замечания пользователя по отсутствующим полям, неполным названиям, городам и ошибкам в определении типа документа для XML.

## Требуется подтверждение пользователя

> [!IMPORTANT]
> Я добавляю новое поле `document_kind` ("Что за документ?"), как вы просили. Оно будет первой строкой в таблице результатов.
>
> [!WARNING]
> Будут применены более строгие правила промпта, чтобы избежать «сухих» названий и гарантировать включение полного названия объекта в поле `title`.

## Предлагаемые изменения

### База знаний

#### [ИЗМЕНИТЬ] [knowledge_base.json](file:///home/segun/CascadeProjects/Перед 0_2/Перед 0/project_analyzer/knowledge_base.json)
- Добавить поле `"document_kind"` во все объекты-примеры для обучения LLM (Few-Shot).

### Бэкенд и Интерфейс

#### [ИЗМЕНИТЬ] [web_app_v5.py](file:///home/segun/CascadeProjects/Перед 0_2/Перед 0/project_analyzer/web_app_v5.py)
- **Промпт-инжиниринг**:
    - Обновить `PROMPT_SYSTEM`, включив `document_kind`.
    - Усилить правило извлечения `title`: «Извлекай МАКСИМАЛЬНО ПОЛНОЕ название, включая объект строительства (например, 'Техническое задание на выполнение работ... МБОУ СОШ')».
    - Усилить правило `developer`: «Всегда ищи город (например, 'г. Электросталь') в блоке адреса и включай его».
    - Исправить `document_type` для XML: явно указать использовать «Структурированный XML-документ (...)», когда анализируется XML.
- **Интерфейс (Vue Template)**:
    - Добавить строку для «Что за документ?» (`document_kind`) в таблицу результатов.
- **Логика API**:
    - Убедиться, что `filename` передается в `call_llm` для лучшего определения типа (PDF или XML).

### Оценка

#### [ИЗМЕНИТЬ] [evaluate_pipeline.py](file:///home/segun/CascadeProjects/Перед 0_2/Перед 0/project_analyzer/evaluate_pipeline.py)
- (Опционально) Обновить для учета нового поля при сравнении или просто использовать для проверки улучшений в других полях.

## План проверки

### Автоматические тесты
- Запустить `python3 evaluate_pipeline.py` для проверки точности исправленных полей (Название, Проектировщик, Тип документа).

### Ручная проверка
- Запустить обновленный `web_app_v5.py`.
- Загрузить `ТЗ.xml` и убедиться, что:
    - Присутствует полное название (с объектом).
    - Присутствует «г. Электросталь».
    - Поле «Что за документ?» заполнено как «Техническое задание».
    - «Тип документа» указан как «Структурированный XML-документ».
- Загрузить `Раздел ПД4-КР.pdf` и убедиться, что:
    - В «Кратком содержании» нет выдуманных данных.
    - В «Подрядчике» указан город.

---

## Статус выполнения (Обновлено)
> [!NOTE]
> План реализован успешно.
> - Ошибка с пустым полем "Что за документ?" была выявлена и устранена в функции `call_llm` внутри `web_app_v5.py` (добавлена переменная `document_kind` в словарь `normalized` с поддержкой синонимов).
> - Улучшена интеграция контекстного обучения (`knowledge_base.json`).
> - Модель корректно возвращает город, структуру проектов (особенно XML), и полные заголовки объектов благодаря ужесточенному System Prompt.
