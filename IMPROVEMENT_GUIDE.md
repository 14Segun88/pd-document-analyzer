# 📊 Анализ результатов бенчмарка и варианты улучшения

## Текущие результаты

| Метрика | Значение | Оценка |
|---------|----------|--------|
| **Скорость** | 0.2 сек/документ | ✅ Отлично (ускорение 250x) |
| **100% извлечение** | 4/68 (6%) | 🔴 КРИТИЧНО НИЗКОЕ |
| **Точность ключей** | 229/408 (56.1%) | 🟡 Ниже цели (75%+) |

## 🔍 Причины низкого качества

1. **Патч не применён** - web_app_v4.py не содержит LLM-интеграцию
2. **Старые паттерны** - используются regex-паттерны без LLM
3. **Отсутствие KB** - Knowledge Base не загружается (путь не найден)
4. **Нет Semantic Matching** - примеры не инжектятся в промпт

## 🎯 3 Варианта исправления

---

### ВАРИАНТ 1: Быстрое исправление (рекомендуется)

**Файл**: `fix_v1_simple.py` → `web_app_v5_llm.py`

**Что делает**:
- ✅ Добавляет LLM-интеграцию
- ✅ Semantic Matching для KB
- ✅ OLMOCR для сканов
- ✅ Fallback на старые паттерны

**Применение**:
```bash
cd /home/segun/CascadeProjects/Перед\ 0_2/.kilo/worktrees/playful-flower
cp web_app_v5_llm.py web_app_v4.py
```

**Ожидаемый результат**: 70-80% точность

---

### ВАРИАНТ 2: Улучшенный JSON-парсинг

**Файл**: `fix_v2_robust_json.py`

**Что делает**:
- ✅ Надёжный парсер JSON (подсчёт скобок)
- ✅ Обработка markdown-обёрток
- ✅ Исправление trailing commas
- ✅ Валидация вложенных объектов

**Интеграция**:
```python
# В web_app_v5_llm.py заменить строки 175-184:
# Было:
json_match = re.search(r'(\{.*\})', clean_text, re.DOTALL)

# Стало:
from fix_v2_robust_json import parse_json_robust
parsed = parse_json_robust(resp)
```

**Ожидаемый результат**: +5-10% точность (устойчивость к ошибкам JSON)

---

### ВАРИАНТ 3: Гибридный подход (максимальное качество)

**Файл**: `fix_v3_hybrid.py`

**Что делает**:
- ✅ LLM-анализ (быстро)
- ✅ Паттерн-анализ (надёжно)
- ✅ Валидация качества
- ✅ Объединение результатов
- ✅ Статистика заполненности

**Преимущества**:
- Гарантированное качество (LLM + паттерны)
- Проверка на галлюцинации
- Автоматическое исправление ошибок

**Ожидаемый результат**: 80-90% точность

---

## 📝 Рекомендуемый план действий

### Шаг 1: Применить ВАРИАНТ 1
```bash
cp web_app_v5_llm.py web_app_v4.py
```

### Шаг 2: Обновить benchmark_models.py
```python
# Изменить путь к web_app_v4.py:
sys.path.insert(0, str(Path('.').absolute()))
```

### Шаг 3: Запустить тест
```bash
python3 benchmark_models.py
```

### Шаг 4: Если качество < 75%
- Интегрировать ВАРИАНТ 2 (robust JSON)
- Или ВАРИАНТ 3 (гибридный подход)

---

## 🔧 Быстрые исправления

### Исправление 1: Путь к knowledge_base.json
```python
# В web_app_v5_llm.py строка 60:
# Было:
with open("knowledge_base.json", "r", encoding="utf-8") as f:

# Стало:
kb_paths = [
    Path("knowledge_base.json"),
    Path(__file__).parent.parent.parent.parent / "knowledge_base.json",
]
for kb_path in kb_paths:
    if kb_path.exists():
        with open(kb_path, "r", encoding="utf-8") as f:
            self.kb_data = json.load(f)
        break
```

### Исправление 2: Temperature для точности
```python
# В _call_llm():
"temperature": 0.05,  # Было 0.1, снизить для точности
```

### Исправление 3: Добавить валидацию
```python
# После получения LLM результата:
if not llm_result.get('title') and not llm_result.get('customer'):
    # LLM не справился - использовать паттерны
    llm_result = self._extract_with_patterns(text)
```

---

## 📊 Ожидаемые результаты после исправлений

| Метрика | Сейчас | После В1 | После В2 | После В3 |
|---------|--------|----------|----------|----------|
| 100% извлечение | 6% | 30-40% | 40-50% | 50-70% |
| Точность ключей | 56% | 70-75% | 75-80% | 80-90% |
| Время/файл | 0.2s | 1-2s | 1-2s | 2-3s |

---

## ✅ Итог

**Рекомендуется**: Начать с ВАРИАНТА 1 (быстрое исправление), затем при необходимости добавить ВАРИАНТ 2 или 3.

**Цель**: Достичь 75%+ точности при сохранении приемлемой скорости (< 3 сек/файл).
